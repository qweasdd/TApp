использование var + 0.75
использование классов для исключений и инициализация их полезной информацией +0.5
реализация несколько конструкторов для классов исключений +0.5
проверка на null перед вызовом делегата +1
использование констант +0.25
использование readonly +0.25
использование switch + 0.5
использование object initializer +.25
использование событый +0.5  
использование jagged массивов вместо многомерных +1
использование static readonly вместо const для полей видимых за пределами данной сборки и const в обратном случае +0.75
вызов виртуальных методов в конструкторе -0.5
метод Equals обратывает аргумент равный null + 0.5
функция возвращает пустую строку(массив) вместо null +0.5
проверка на чётность с помощью логического и + 0.5
использование большого количества локальных переменных -0.5
использование стуктур занимающих много памяти -0.5
повторный вызов операторов as, is вместо сохранения результата в переменную -0.5
использование свойств возращающих массивы -0.5
ненужная инициализация полей -0.25
ненужная распаковка value type -0.75
неиспользуемые параметры функций -0.5
проверка строки на пустоту с помощью  myString.Length или String.IsNullOrEmpty + 0.25
игнорирование возвращающих значений функций -0.5
реализация Equals(x) вместе с Equals(object) +0.25
использование Min и Max вместо сравнений +0.25
переопределение Equals и GetHashCode для структур +0.5
при наличии перегрузок метода для строки и для символа использовать второй если строка единичной длины +0.25
использование Environment.GetFolderPath +0.25
объявление статическими классы содержащие только статические члены +0.25
использование оператора == для типов с плавующей точкой -0.5
проверка на null параметров видимых методов +0.5
сравнение с NaN -0.25
использование static методов +0.25
ожидание double после деления integer -0.5


доля строк комментариев
средняя длинна комментария
среднее число строк для функций (классов)
метрики Холстеда
метрики Джилба
и тд

использование Expression Bodied Members +0.25
использование операторов ?. , ?? +0.25
использование интерполяции строк вместо String.Format +0.25
использование лямбда выражений +0.25
использование условного оператора +0.5

I перед именем интерфейса +0.25
и другие правила наименования 